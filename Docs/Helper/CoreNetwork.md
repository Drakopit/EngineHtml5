# CoreNetwork API

<!-- Generated by Tools/generate-docs.js. Edit source JSDoc, then run npm run docs. -->

Transport adapters and lightweight reusable online gameplay helpers.

Documented exports: **10/10**.

| Symbol | Kind | Summary |
| --- | --- | --- |
| [`ChatManager`](#chatmanager) | `class` | Reusable online text chat service connected to a `GameNetwork` session. It owns message normalization and system notices, while a game supplies its own canvas UI such as `ChatWindow`. |
| [`GameNetwork`](#gamenetwork) | `class` | High-level multiplayer session API built on a replaceable transport adapter. `GameNetwork` discovers peers in a room and forwards game-defined messages. It does not contain maps, entities or gameplay rules. |
| [`LocalNetworkAdapter`](#localnetworkadapter) | `class` | Same-origin multiplayer transport implemented with `BroadcastChannel`. It is useful for local demos and testing two browser tabs without an MMO backend. Messages remain scoped to a room and do not leave the browser. |
| [`NETWORK_CLIENT_STATE`](#network_client_state) | `const` | Lifecycle states emitted by `NetworkClient`. |
| [`NetworkClient`](#networkclient) | `class` | Low-level WebSocket client for message envelopes and optional request/reply calls. Games normally consume `GameNetwork` and an adapter instead of this transport primitive directly. |
| [`NetworkMessage`](#networkmessage) | `class` | Creates and decodes transport-neutral message envelopes. Payload contents belong to the game; this utility only gives messages a stable `type` and metadata shape. |
| [`NetworkStateSync`](#networkstatesync) | `class` | Periodically broadcasts snapshots of small tracked objects through a network client. This generic helper reads and writes selected fields only; it has no knowledge of entities, maps or simulation. |
| [`OnlineEntitySync`](#onlineentitysync) | `class` | Rate-limited sender for one game's local entity state. The game supplies `readState`, choosing the compact fields that remote peers need to render; unchanged snapshots are not resent. |
| [`OnlinePlayerManager`](#onlineplayermanager) | `class` | Stores remote peer render state and smooths numeric movement fields. The manager is intentionally representation-neutral: the demo decides how each returned remote player is rendered. |
| [`WebSocketClientAdapter`](#websocketclientadapter) | `class` | Adapter that carries `GameNetwork` messages through an external WebSocket relay. The relay is optional and replaceable; the game still owns world state and rules. |

## ChatManager

`class` from [`CoreNetwork/Online/ChatManager.js`](../../CoreNetwork/Online/ChatManager.js)

Reusable online text chat service connected to a `GameNetwork` session. It owns message normalization and system notices, while a game supplies its own canvas UI such as `ChatWindow`.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `network` | `GameNetwork` | Active high-level network session. |
| `[options]` | `Object` | Chat constraints. |
| `[options.maxLength=120]` | `number` | Maximum transmitted text length. |

### Documented Methods

#### `sendMessage()`

Emits a local chat message and relays it to connected peers.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `text` | `string` | User-entered message. |

**Returns:** `boolean` - Whether non-empty text was accepted.
#### `onMessageReceived()`

Subscribes to local, remote and system chat messages.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `callback` | `Function` | Receives a normalized chat message. |

**Returns:** `Function` - Unsubscribe callback.
#### `addSystemMessage()`

Emits an informational message without transmitting it.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `text` | `string` | Notice content. |

**Returns:** `void`
#### `dispose()`

Releases subscriptions created by this service.

**Returns:** `void`

## GameNetwork

`class` from [`CoreNetwork/GameNetwork.js`](../../CoreNetwork/GameNetwork.js)

High-level multiplayer session API built on a replaceable transport adapter. `GameNetwork` discovers peers in a room and forwards game-defined messages. It does not contain maps, entities or gameplay rules.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[options]` | `Object` | Initial network settings. |
| `[options.adapter=null]` | `Object\|null` | Adapter implementing connect, disconnect and send. |
| `[options.roomId="main"]` | `string` | Logical room shared by connected peers. |
| `[options.peer={}]` | `Object` | Local peer profile such as a display name. |

**Example**

```js
const network = new GameNetwork({
adapter: new LocalNetworkAdapter(),
peer: { name: "Ada" },
});
network.onPlayerStateReceived((peerId, state) => players.updateState(peerId, state));
network.connect();
```

### Documented Methods

#### `useAdapter()`

Selects the transport used by this session.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `adapter` | `Object` | A LocalNetworkAdapter, WebSocketClientAdapter or compatible adapter. |

**Returns:** `GameNetwork` - This network instance.
#### `connect()`

Opens the selected adapter and starts peer discovery.

**Returns:** `GameNetwork` - This network instance.
#### `disconnect()`

Closes the adapter and forgets discovered remote peers.

**Returns:** `GameNetwork` - This network instance.
#### `setPeerProfile()`

Updates the local public identity and informs connected peers.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `profile` | `Object` | Public peer fields such as `name`. |

**Returns:** `GameNetwork` - This network instance.
#### `send()`

Sends an application message to peers in the current room.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `type` | `string` | Message channel name. |
| `[payload={}]` | `Object` | Game-owned serializable payload. |

**Returns:** `boolean` - Whether an adapter accepted the message.
#### `sendPlayerState()`

Sends the local player's small replicated state payload.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `state` | `Object` | Game-selected state such as position and animation. |

**Returns:** `boolean` - Whether the message was submitted.

## LocalNetworkAdapter

`class` from [`CoreNetwork/Adapters/LocalNetworkAdapter.js`](../../CoreNetwork/Adapters/LocalNetworkAdapter.js)

Same-origin multiplayer transport implemented with `BroadcastChannel`. It is useful for local demos and testing two browser tabs without an MMO backend. Messages remain scoped to a room and do not leave the browser.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[options]` | `Object` | Adapter settings. |
| `[options.roomId="main"]` | `string` | Channel room identifier. |
| `[options.peer={}]` | `Object` | Local public peer profile. |
| `[options.channelPrefix="gameforgejs-online"]` | `string` | Browser channel namespace. |

### Documented Methods

#### `connect()`

Opens the browser broadcast channel.

**Returns:** `LocalNetworkAdapter` - This adapter.
#### `send()`

Broadcasts an envelope to the other tabs in this room.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `type` | `string` | Message type. |
| `[payload={}]` | `Object` | Serializable message body. |

**Returns:** `boolean` - Whether the channel was open.
#### `disconnect()`

Announces departure and releases the browser channel.

**Returns:** `LocalNetworkAdapter` - This adapter.

## NETWORK_CLIENT_STATE

`const` from [`CoreNetwork/NetworkClient.js`](../../CoreNetwork/NetworkClient.js)

Lifecycle states emitted by `NetworkClient`.

### Documented Methods

#### `static FromConfig()`

Constructs a client using `Config.data.network` compatible properties.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[config]` | `Object` | Network configuration object. |

**Returns:** `NetworkClient` - Configured client.
#### `Connect()`

Opens a WebSocket connection.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[url]` | `string` | Endpoint override. |
| `[protocols]` | `string\|string[]` | WebSocket subprotocol override. |

**Returns:** `NetworkClient` - This client.
#### `Disconnect()`

Stops reconnection and closes the socket.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[code=1000]` | `number` | WebSocket close code. |
| `[reason="client disconnect"]` | `string` | Close reason. |

**Returns:** `NetworkClient` - This client.
#### `Send()`

Sends a message envelope or creates one from a type and payload.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `type` | `string\|Object` | Type name or complete envelope. |
| `[payload={}]` | `Object` | Serializable payload. |
| `[meta={}]` | `Object` | Envelope metadata. |

**Returns:** `Object|boolean` - Sent envelope, or `false` while disconnected.
#### `Request()`

Sends a correlated message and waits for its matching reply.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `type` | `string` | Request message type. |
| `[payload={}]` | `Object` | Serializable request data. |
| `[options]` | `Object` | Timeout and expected response type. |

**Returns:** `Promise<Object>` - Reply envelope.
#### `Reply()`

Responds to a previously received request envelope.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `requestMessage` | `Object` | Envelope carrying correlation metadata. |
| `type` | `string` | Response type. |
| `[payload={}]` | `Object` | Serializable response data. |
| `[meta={}]` | `Object` | Additional response metadata. |

**Returns:** `Object|boolean` - Sent response or `false`.

## NetworkClient

`class` from [`CoreNetwork/NetworkClient.js`](../../CoreNetwork/NetworkClient.js)

Low-level WebSocket client for message envelopes and optional request/reply calls. Games normally consume `GameNetwork` and an adapter instead of this transport primitive directly.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[options]` | `Object` | Client connection settings. |
| `[options.url=null]` | `string\|null` | WebSocket endpoint. |
| `[options.autoConnect=false]` | `boolean` | Connect during construction. |
| `[options.autoReconnect=false]` | `boolean` | Retry unexpected connection losses. |
| `[options.serializer=null]` | `Function\|null` | Envelope-to-wire converter. |
| `[options.parser=null]` | `Function\|null` | Wire-to-envelope converter. |

### Documented Methods

#### `static FromConfig()`

Constructs a client using `Config.data.network` compatible properties.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[config]` | `Object` | Network configuration object. |

**Returns:** `NetworkClient` - Configured client.
#### `Connect()`

Opens a WebSocket connection.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[url]` | `string` | Endpoint override. |
| `[protocols]` | `string\|string[]` | WebSocket subprotocol override. |

**Returns:** `NetworkClient` - This client.
#### `Disconnect()`

Stops reconnection and closes the socket.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[code=1000]` | `number` | WebSocket close code. |
| `[reason="client disconnect"]` | `string` | Close reason. |

**Returns:** `NetworkClient` - This client.
#### `Send()`

Sends a message envelope or creates one from a type and payload.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `type` | `string\|Object` | Type name or complete envelope. |
| `[payload={}]` | `Object` | Serializable payload. |
| `[meta={}]` | `Object` | Envelope metadata. |

**Returns:** `Object|boolean` - Sent envelope, or `false` while disconnected.
#### `Request()`

Sends a correlated message and waits for its matching reply.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `type` | `string` | Request message type. |
| `[payload={}]` | `Object` | Serializable request data. |
| `[options]` | `Object` | Timeout and expected response type. |

**Returns:** `Promise<Object>` - Reply envelope.
#### `Reply()`

Responds to a previously received request envelope.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `requestMessage` | `Object` | Envelope carrying correlation metadata. |
| `type` | `string` | Response type. |
| `[payload={}]` | `Object` | Serializable response data. |
| `[meta={}]` | `Object` | Additional response metadata. |

**Returns:** `Object|boolean` - Sent response or `false`.

## NetworkMessage

`class` from [`CoreNetwork/NetworkMessage.js`](../../CoreNetwork/NetworkMessage.js)

Creates and decodes transport-neutral message envelopes. Payload contents belong to the game; this utility only gives messages a stable `type` and metadata shape.

### Documented Methods

#### `static Create()`

Creates an envelope with an id and send timestamp.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `type` | `string` | Application message type. |
| `[payload={}]` | `Object` | Serializable body. |
| `[meta={}]` | `Object` | Additional metadata. |

**Returns:** `Object` - Message envelope.
#### `static IsEnvelope()`

Tests whether a value has the minimal envelope contract.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `value` | `*` | Candidate value. |

**Returns:** `boolean` - Whether the value is an envelope.
#### `static Serialize()`

Encodes an envelope as JSON.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `message` | `Object` | Message envelope. |

**Returns:** `string` - JSON representation.
#### `static Parse()`

Parses JSON text, an ArrayBuffer or an existing envelope.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `data` | `string\|ArrayBuffer\|Object` | Wire data. |

**Returns:** `Object` - Message envelope.

## NetworkStateSync

`class` from [`CoreNetwork/NetworkStateSync.js`](../../CoreNetwork/NetworkStateSync.js)

Periodically broadcasts snapshots of small tracked objects through a network client. This generic helper reads and writes selected fields only; it has no knowledge of entities, maps or simulation.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[options]` | `Object` | Synchronization settings. |
| `[options.client=null]` | `NetworkClient\|null` | Client used to send snapshots. |
| `[options.channel="state:sync"]` | `string` | Envelope type for snapshots. |
| `[options.id]` | `string` | Local source identifier. |
| `[options.tickRate=20]` | `number` | Snapshot broadcasts per second. |

### Documented Methods

#### `Track()`

Tracks state with game-provided reader and optional writer callbacks.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `id` | `string` | Entity identifier inside snapshots. |
| `callbacks` | `Object` | State callbacks. |
| `callbacks.read` | `Function` | Produces serializable state. |
| `[callbacks.write=null]` | `Function\|null` | Applies remote state. |

**Returns:** `NetworkStateSync` - This synchronizer.
#### `TrackObject()`

Tracks common fields on a plain target object.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `id` | `string` | Entity identifier. |
| `target` | `Object` | Target object. |
| `[fields]` | `string[]` | Replicated property names. |

**Returns:** `NetworkStateSync` - This synchronizer.
#### `Snapshot()`

Produces the current serializable entity snapshot.

**Returns:** `Object` - Snapshot containing source, timestamp and entities.
#### `Broadcast()`

Sends one current snapshot.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[meta={}]` | `Object` | Message envelope metadata. |

**Returns:** `Object|boolean` - Sent envelope or `false` without a client.

## OnlineEntitySync

`class` from [`CoreNetwork/Online/OnlineEntitySync.js`](../../CoreNetwork/Online/OnlineEntitySync.js)

Rate-limited sender for one game's local entity state. The game supplies `readState`, choosing the compact fields that remote peers need to render; unchanged snapshots are not resent.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `network` | `GameNetwork` | Session used to send player state. |
| `options` | `Object` | Entity synchronization settings. |
| `options.readState` | `Function` | Returns serializable local state. |
| `[options.syncRate=12]` | `number` | Maximum sends per second. |

### Documented Methods

#### `update()`

Advances the rate limiter and sends changed state when due.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[deltaTime=0.0166667]` | `number` | Frame time in seconds. |

**Returns:** `boolean` - Whether state was sent.
#### `send()`

Immediately evaluates and optionally sends local state.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[force=false]` | `boolean` | Send even when fields did not change. |

**Returns:** `boolean` - Whether the network accepted a state message.

## OnlinePlayerManager

`class` from [`CoreNetwork/Online/OnlinePlayerManager.js`](../../CoreNetwork/Online/OnlinePlayerManager.js)

Stores remote peer render state and smooths numeric movement fields. The manager is intentionally representation-neutral: the demo decides how each returned remote player is rendered.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `network` | `GameNetwork` | Session supplying peer and player-state events. |
| `[options]` | `Object` | Remote state settings. |
| `[options.smoothing=16]` | `number` | Interpolation response per second. |

### Documented Methods

#### `spawn()`

Creates or refreshes a remote player profile.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `peer` | `Object` | Peer with a stable `id`. |

**Returns:** `Object|null` - Stored player entry.
#### `updateState()`

Stores the newest target state received from a remote player.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `peerId` | `string` | Remote peer identifier. |
| `[state={}]` | `Object` | Target render state. |
| `[peer={}]` | `Object` | Updated peer profile. |

**Returns:** `void`
#### `update()`

Interpolates displayed numeric position and velocity fields.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[deltaTime=0.0166667]` | `number` | Frame time in seconds. |

**Returns:** `void`
#### `getPlayers()`

Returns render-ready remote player records.

**Returns:** `Object[]` - Visible interpolated player entries.
#### `dispose()`

Removes subscriptions and known remote peers.

**Returns:** `void`

## WebSocketClientAdapter

`class` from [`CoreNetwork/Adapters/WebSocketClientAdapter.js`](../../CoreNetwork/Adapters/WebSocketClientAdapter.js)

Adapter that carries `GameNetwork` messages through an external WebSocket relay. The relay is optional and replaceable; the game still owns world state and rules.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[options]` | `Object` | Adapter settings. |
| `[options.url=null]` | `string\|null` | WebSocket relay URL. |
| `[options.roomId="main"]` | `string` | Logical room identifier. |
| `[options.peer={}]` | `Object` | Local peer profile. |
| `[options.autoReconnect=true]` | `boolean` | Whether the low-level client reconnects. |

### Documented Methods

#### `connect()`

Creates and connects the underlying WebSocket client.

**Returns:** `WebSocketClientAdapter` - This adapter.
#### `send()`

Sends a room-aware message envelope to the relay.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `type` | `string` | Message type. |
| `[payload={}]` | `Object` | Serializable message body. |

**Returns:** `boolean|Object` - `false` while offline, otherwise the sent envelope.
#### `disconnect()`

Gracefully closes the underlying client.

**Returns:** `WebSocketClientAdapter` - This adapter.
