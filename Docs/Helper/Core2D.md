# Core2D API

<!-- Generated by Tools/generate-docs.js. Edit source JSDoc, then run npm run docs. -->

Canvas rendering, collisions, gameplay objects and canvas UI APIs.

Documented exports: **72/72**.

| Symbol | Kind | Summary |
| --- | --- | --- |
| [`ActionInputComponent`](#actioninputcomponent) | `class` | Exposes configured `ActionManager` values to a composed game entity. |
| [`ActionManager`](#actionmanager) | `class` | Maps game actions to one or more low-level keyboard or gamepad inputs. Use this layer in gameplay so controls can be configured per game while the `Input` class remains responsible for raw per-frame state. |
| [`Animator`](#animator) | `class` | Controla a transição de diferentes animações para um mesmo Sprite. |
| [`AssetManager`](#assetmanager) | `class` | Preloader para imagens, áudios e dados JSON. Evita travamentos durante o gameplay. |
| [`aStar`](#astar) | `function` | Finds a shortest grid path using a Manhattan-distance A-star search. |
| [`AudioManager`](#audiomanager) | `class` | Singleton Web Audio service for loaded effects, music and master volume control. |
| [`Base`](#base) | `class` | That class was made, to compose the EngineHtml5 framework. Base class, that load all need methods |
| [`bfs`](#bfs) | `function` | 🔵 1️⃣ BFS (Breadth-First Search) Explora em largura ✅ Garante menor caminho |
| [`BootstrapGame`](#bootstrapgame) | `function` | Loads a game's configuration and assets, registers its levels and starts the engine. |
| [`BoundsComponent`](#boundscomponent) | `class` | Supplies a reusable AABB for collision tests and pointer selection. |
| [`BoxController2D`](#boxcontroller2d) | `class` | Resolves an entity's manifest-defined collision, hurt and attack boxes in world space. |
| [`BoxManifest2D`](#boxmanifest2d) | `class` | Parses data-driven collision, hurtbox and hitbox definitions for animated entities. |
| [`Button`](#button) | `class` | Canvas-rendered clickable UI button. |
| [`Camera`](#camera) | `class` | Viewport camera for translating, zooming and shaking a 2D game world. |
| [`CharCode`](#charcode) | `const` | Legacy numeric key codes mapped back to readable names. |
| [`ChatWindow`](#chatwindow) | `class` | Canvas-rendered chat panel composed from GameForgeJS drawing and text input APIs. The window is transport-neutral: connect it to `ChatManager` or any local message source through `onMessageSubmitted` and `addMessage`. |
| [`Collide2D`](#collide2d) | `class` | Provides methods for detecting collisions between 2D shapes. Includes methods for axis-aligned bounding box (AABB) collisions, circular collisions, and point-in-rectangle checks. |
| [`CombatResolver2D`](#combatresolver2d) | `class` | Stateless hitbox-versus-hurtbox resolver for data-driven 2D combat. |
| [`ComboController`](#combocontroller) | `class` | Small attack-chain state machine driven by a game's animation frames. |
| [`Component`](#component) | `class` | Base attachable behavior unit for composing lightweight game entities. |
| [`Config`](#config) | `class` | Loads and merges global and per-game JSON configuration files. |
| [`DEBUG`](#debug) | `const` | Reads whether collision and hitbox debugging is enabled in config. |
| [`DebugMap`](#debugmap) | `class` | Small debug tile-map level helper rendered on a 2D screen. |
| [`Deprecation`](#deprecation) | `class` | Reports use of compatibility APIs once per deprecated identifier. This keeps legacy projects running while guiding new games toward the maintained GameForgeJS API. |
| [`dfs`](#dfs) | `function` | 🔵 2️⃣ DFS (Depth-First Search) Explora até o fundo ❌ Não garante menor caminho |
| [`dijkstra`](#dijkstra) | `function` | 🔵 3️⃣ Dijkstra Explora com base em pesos ✅ Garante menor caminho Mais pesado que BFS |
| [`Draw`](#draw) | `class` | Provides drawing utilities for 2D graphics. This class facilitates drawing operations on a given canvas, including text, rectangles, circles, and sprites. |
| [`Engine`](#engine) | `class` | Main update and render loop orchestrator for the active GameForgeJS level. The engine publishes frame events consumed by systems such as `Input`. |
| [`EventEmitter`](#eventemitter) | `class` | Minimal synchronous event publisher used across engine services. |
| [`FloatingText`](#floatingtext) | `class` | Canvas effect for short-lived labels such as damage or collectible feedback. |
| [`GameObject`](#gameobject) | `class` | Base movable 2D entity with sprite, collision and component support. Extend it for game-specific players, enemies and interactive objects. |
| [`GamePad`](#gamepad) | `class` | Low-level browser gamepad state. Events are useful notifications, but the current state is always reconciled from navigator.getGamepads() each frame. |
| [`GamepadAlias`](#gamepadalias) | `class` | Resolves readable controller button and axis names into browser Gamepad API indices. Profiles allow the same game's actions to match Xbox, PlayStation or Nintendo labels. |
| [`GameViewport`](#gameviewport) | `class` | Applies the responsive full-window canvas mode selected by a game config. Logical canvas coordinates remain unchanged; only presentation fills the available browser viewport. |
| [`getNeighbors`](#getneighbors) | `function` | Returns walkable cardinal neighbors for a grid coordinate. |
| [`HealthComponent`](#healthcomponent) | `class` | Tracks hit points, invulnerability time and owner damage/death callbacks. |
| [`Input`](#input) | `class` | Manages per-frame keyboard, mouse, touch and polled gamepad input. Gamepad state is polled during the engine update so controllers already available to the browser are detected without reconnecting. |
| [`JsonManifestComposer`](#jsonmanifestcomposer) | `class` | Combines JSON game manifests referenced through their `compose` entries. |
| [`key`](#key) | `function` | Encodes a coordinate for pathfinding maps. |
| [`KeyCode`](#keycode) | `const` | Legacy-friendly names mapped to browser keyboard `code` values. |
| [`Label`](#label) | `class` | Simple canvas text label positioned by a rectangle. |
| [`Level`](#level) | `class` | Base scene lifecycle container updated and drawn by the engine. Extend this class to own a game's entities and canvas screens. |
| [`LevelHandler`](#levelhandler) | `const` | Ordered level registry and current-level cursor used by `Engine`. |
| [`LifetimeComponent`](#lifetimecomponent) | `class` | Disables or removes short-lived owner objects after an elapsed duration. |
| [`Logger`](#logger) | `class` | Console logging helpers for engine startup, diagnostics and HTTP requests. |
| [`Mat4`](#mat4) | `class` | Dependency-free four-by-four matrix math utilities for Render3D. |
| [`MathExt`](#mathext) | `class` | General scalar and 2D interpolation helpers shared by demos. |
| [`Menu`](#menu) | `class` | Basic keyboard-controlled menu level supplied as a simple 2D example. |
| [`Mouse`](#mouse) | `class` | Manages mouse input for the game framework. |
| [`NineSlice`](#nineslice) | `class` | Draws resizable UI panels from a nine-slice source image. |
| [`ObjectPool`](#objectpool) | `class` | Recicla GameObjects (como Tiros e Partículas) para evitar vazamento de memória. |
| [`Particle`](#particle) | `class` | Short-lived moving dot used by the basic canvas particle system. |
| [`ParticleSystem`](#particlesystem) | `class` | Collection that emits, updates and draws simple `Particle` instances. |
| [`Physic2D`](#physic2d) | `class` | Static helpers and coefficient values for basic 2D motion calculations. |
| [`reconstructPath`](#reconstructpath) | `function` | Reconstructs a discovered route from predecessor entries. |
| [`Rectangle`](#rectangle) | `class` | Axis-aligned 2D rectangle used for canvas layout and collision bounds. |
| [`ResourceManifestLoader`](#resourcemanifestloader) | `class` | Queues images, audio, shaders, models and JSON declared by game resource manifests. |
| [`Scene`](#scene) | `class` | This class is part of the EngineHtml5 framework and provides functionality to load and render 2D scenes based on maps created with the Tiled tool. |
| [`Screen`](#screen) | `class` | Creates and owns a two-dimensional gameplay canvas and draw context. |
| [`ScreenUI`](#screenui) | `class` | Transparent overlay screen for GameForgeJS canvas-based UI above gameplay. |
| [`Sprite`](#sprite) | `class` | Canvas sprite renderer supporting sheet-based animation frames. |
| [`Store`](#store) | `class` | This class is part of the EngineHtml5 framework and provides functionality to save and restore game state in the browser's local storage. |
| [`TextBox`](#textbox) | `class` | Canvas-drawn editable text field for in-game GameForgeJS UI. |
| [`TileCollision2D`](#tilecollision2d) | `class` | Tile-map collision queries and axis-separated movement for rectangular actors. |
| [`Touch`](#touch) | `class` | Manages touch input for the game framework. |
| [`TransformComponent`](#transformcomponent) | `class` | Stores 2D position, rotation and scale and can mirror them onto its owner. |
| [`UI`](#ui) | `class` | Convenience factory for canvas buttons, labels and text boxes on a screen. |
| [`UIWindow`](#uiwindow) | `class` | Nine-slice canvas window that positions child labels relative to its frame. Subclass or compose this class for in-game panels rendered inside GameForgeJS rather than as external HTML. |
| [`Util`](#util) | `class` | That class was made, to compose the EngineHtml5 framework. |
| [`Vector2D`](#vector2d) | `class` | That class was made, to compose the EngineHtml5 framework. |
| [`Vector3D`](#vector3d) | `class` | That class was made, to compose the EngineHtml5 framework. |
| [`VelocityComponent`](#velocitycomponent) | `class` | Applies velocity, gravity and friction to an owner's 2D position. |

## ActionInputComponent

`class` from [`CoreCross/Component.js`](../../CoreCross/Component.js)

Exposes configured `ActionManager` values to a composed game entity.

## ActionManager

`class` from [`CoreCross/Input/ActionManager.js`](../../CoreCross/Input/ActionManager.js)

Maps game actions to one or more low-level keyboard or gamepad inputs. Use this layer in gameplay so controls can be configured per game while the `Input` class remains responsible for raw per-frame state.

**Example**

```js
ActionManager.MapAction("JUMP", [
Input.Keyboard("Space"),
Input.GamepadButton("A"),
]);
if (ActionManager.IsActionDown("JUMP")) player.Jump();
```

### Documented Methods

#### `static LoadMappings()`

Replaces all mappings using values loaded from a game config.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `configMappings` | `Object` | Map of action names to input rules. |

**Returns:** `void`
#### `static MapAction()`

Assigns rules for one action.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `actionName` | `string` | Game-defined action name. |
| `[rules=[]` | `Object[]` | ] - Rules produced by `Input.Keyboard` or gamepad helpers. |

**Returns:** `typeof ActionManager` - This action manager class.
#### `static IsActionDown()`

Tests whether any mapped input was pressed this frame.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `actionName` | `string` | Game-defined action. |

**Returns:** `boolean` - Whether the action went down.
#### `static IsActionUp()`

Tests whether any mapped input was released this frame.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `actionName` | `string` | Game-defined action. |

**Returns:** `boolean` - Whether the action went up.
#### `static IsAction()`

Tests whether an action currently exceeds its digital threshold.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `actionName` | `string` | Game-defined action. |

**Returns:** `boolean` - Whether the action is held.
#### `static GetActionValue()`

Reads the greatest mapped input strength, retaining analog axis intensity.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `actionName` | `string` | Game-defined action. |

**Returns:** `number` - Action value from zero to one.

## Animator

`class` from [`Core2D/Graphics/Animator.js`](../../Core2D/Graphics/Animator.js)

Controla a transição de diferentes animações para um mesmo Sprite.

### Documented Methods

#### `AddAnimation()`

Registra uma nova animação

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `name` | `string` | Nome da animação (ex: "Idle", "Run") |
| `row` | `number` | Linha na spritesheet |
| `frameCount` | `number` | Quantidade de frames |
| `speed` | `number` | Velocidade da animação (updatesPerFrame) |
#### `Play()`

Toca a animação se ela já não estiver tocando

## AssetManager

`class` from [`CoreCross/Assets/AssetManager.js`](../../CoreCross/Assets/AssetManager.js)

Preloader para imagens, áudios e dados JSON. Evita travamentos durante o gameplay.

### Documented Methods

#### `QueueImage()`

Coloca uma imagem na fila de carregamento
#### `QueueShader()`

Coloca um shader na fila de carregamento
#### `GetShader()`

Retorna o código fonte do shader solicitado

## aStar

`function` from [`CoreCross/Pathfinding/AStar.js`](../../CoreCross/Pathfinding/AStar.js)

Finds a shortest grid path using a Manhattan-distance A-star search.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `grid` | `number[][]` | Grid whose zero entries can be walked. |
| `start` | `Object` | Starting `{x, y}` coordinate. |
| `goal` | `Object` | Destination `{x, y}` coordinate. |

**Returns:** `Object[]|null` - Ordered path or `null` when unreachable.

## AudioManager

`class` from [`CoreCross/Audio/AudioManager.js`](../../CoreCross/Audio/AudioManager.js)

Singleton Web Audio service for loaded effects, music and master volume control.

### Documented Methods

#### `PlaySFX()`

Toca um Efeito Sonoro (SFX). Fire and Forget! Cria uma instância nova, toca e o próprio navegador limpa da memória depois.
#### `PlayBGM()`

Toca Música de Fundo (BGM). Loop automático e permite pausar.

## Base

`class` from [`CoreCross/Base.js`](../../CoreCross/Base.js)

That class was made, to compose the EngineHtml5 framework. Base class, that load all need methods

**Example**

```js
This is a static class, is used to inheritance
```

## bfs

`function` from [`CoreCross/Pathfinding/BFS.js`](../../CoreCross/Pathfinding/BFS.js)

🔵 1️⃣ BFS (Breadth-First Search) Explora em largura ✅ Garante menor caminho

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `grid` | `*` |  |
| `start` | `*` |  |
| `goal` | `*` |  |

## BootstrapGame

`function` from [`CoreCross/Bootstrap.js`](../../CoreCross/Bootstrap.js)

Loads a game's configuration and assets, registers its levels and starts the engine.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[options]` | `Object` | Game startup values. |
| `[options.configPath="gameforge.config.json"]` | `string\|string[]` | Config manifest paths. |
| `[options.manifestPath=null]` | `string\|string[]\|null` | Resource manifest paths. |
| `[options.levels=[]` | `Level[]` | ] - Levels registered before startup. |
| `[options.beforeStart=null]` | `Function\|null` | Async customization hook. |

**Returns:** `Promise<Object>` - Loaded config, assets, manifest and levels.

## BoundsComponent

`class` from [`CoreCross/Component.js`](../../CoreCross/Component.js)

Supplies a reusable AABB for collision tests and pointer selection.

## BoxController2D

`class` from [`Core2D/Collision/BoxController2D.js`](../../Core2D/Collision/BoxController2D.js)

Resolves an entity's manifest-defined collision, hurt and attack boxes in world space.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `owner` | `GameObject` | Animated entity that supplies position and facing. |
| `[manifestData={}]` | `Object\|BoxManifest2D` | Box definitions. |

### Documented Methods

#### `GetHitBoxes()`

Returns active attack boxes for the current animation frame.

**Returns:** `Object[]` - World-space hitboxes.

## BoxManifest2D

`class` from [`Core2D/Collision/BoxManifest2D.js`](../../Core2D/Collision/BoxManifest2D.js)

Parses data-driven collision, hurtbox and hitbox definitions for animated entities.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[data={}]` | `Object` | Box manifest loaded from game assets. |

### Documented Methods

#### `GetBoxes()`

Finds active boxes for a type, animation and current frame.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `type` | `string` | `collision`, `hurt` or `hit`. |
| `[animationName="*"]` | `string` | Active animation. |
| `[frame=0]` | `number` | Active animation frame. |

**Returns:** `Object[]` - Matching local box definitions.

## Button

`class` from [`Core2D/UI/Button.js`](../../Core2D/UI/Button.js)

Canvas-rendered clickable UI button.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `screen` | `Screen` | Screen used for drawing and pointer input. |

### Documented Methods

#### `Click()`

Executes the provided callback function when the button is clicked.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `callback` | `function` |  |

**Returns:** `void`
#### `DrawCursor()`

Draws the button UI with the specified rectangle.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `rect` | `Rectangle` |  |

**Returns:** `void`

## Camera

`class` from [`Core2D/Camera/Camera.js`](../../Core2D/Camera/Camera.js)

Viewport camera for translating, zooming and shaking a 2D game world.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `position` | `Vector2D` | Initial world position. |
| `size` | `Vector2D` | Visible viewport size. |

### Documented Methods

#### `Init()`

Initializes the camera with screen and game world.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `screen` | `Object` | The screen or canvas where the game is rendered. |
| `GameWorld` | `Object` | The game world object. |
#### `LookAt()`

Centers the camera view on the given object.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `Obj` | `Object` | The object to center the camera on. |
#### `ZoomIn()`

Zooms in by a given factor.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `value` | `number` | Zoom factor. |
#### `ZoomOut()`

Zooms out by a given factor.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `value` | `number` | Zoom factor. |
#### `StrictLimit()`

Constrains the camera within the game world boundaries.
#### `Begin()`

Begins camera transformation for rendering.
#### `End()`

Ends camera transformation after rendering.
#### `Update()`

Updates the camera position and zoom based on the given object.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `char` | `Object` | The character or object to focus the camera on. |
#### `LeftEdge()`

Documented method.

**Returns:** `number` - The left edge position of the camera view.
#### `RightEdge()`

Documented method.

**Returns:** `number` - The right edge position of the camera view.
#### `TopEdge()`

Documented method.

**Returns:** `number` - The top edge position of the camera view.
#### `BottomEdge()`

Documented method.

**Returns:** `number` - The bottom edge position of the camera view.

## CharCode

`const` from [`CoreCross/Input/KeyCode.js`](../../CoreCross/Input/KeyCode.js)

Legacy numeric key codes mapped back to readable names.

## ChatWindow

`class` from [`Core2D/UI/ChatWindow.js`](../../Core2D/UI/ChatWindow.js)

Canvas-rendered chat panel composed from GameForgeJS drawing and text input APIs. The window is transport-neutral: connect it to `ChatManager` or any local message source through `onMessageSubmitted` and `addMessage`.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `screen` | `Screen` | Canvas screen used for drawing and pointer coordinates. |
| `[options]` | `Object` | Window position, size and message limits. |

### Documented Methods

#### `onMessageSubmitted()`

Registers a handler for text confirmed by the player.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `callback` | `Function` | Handler receiving the submitted string. |

**Returns:** `Function` - Unsubscribe callback.
#### `addMessage()`

Appends a normalized message for rendering.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `message` | `Object` | Message containing `text`, name and optional system flag. |

**Returns:** `void`
#### `Dispose()`

Removes input listeners when a scene closes.

**Returns:** `void`

## Collide2D

`class` from [`Core2D/Collision/Collide2D.js`](../../Core2D/Collision/Collide2D.js)

Provides methods for detecting collisions between 2D shapes. Includes methods for axis-aligned bounding box (AABB) collisions, circular collisions, and point-in-rectangle checks.

**Returns:** `Object`

**Example**

```js
const collide = new Collide2D();
if (collide.isCollidingAABB(obj1, obj2)) {
console.log("Collision detected");
}
```

### Documented Methods

#### `static isCollidingAABB()`

Checks for collision between two axis-aligned bounding boxes (AABB).

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `obj1` | `Object` | The first rectangle object with position and size properties. |
| `obj2` | `Object` | The second rectangle object with position and size properties. |

**Returns:** `boolean` - True if there is a collision, false otherwise.
#### `static isCollidingCircle()`

Checks for collision between a circle and an object.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `circle` | `Object` | The circle object with x, y, and radius (r) properties. |
| `obj` | `Object` | The object to check collision with, should have x, y properties. |

**Returns:** `boolean` - True if there is a collision, false otherwise.
#### `static isCollidingPoint()`

Checks if a point is inside a rectangle.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `position` | `Object` | The point position with x and y properties. |
| `rect` | `Object` | The rectangle object with x, y, width, and height properties. |

**Returns:** `boolean` - True if the point is inside the rectangle, false otherwise.
#### `static ResolveCollisionAABB()`

Resolve a colisão empurrando o obj1 para fora do obj2

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `obj1` | `GameObject` | Objeto dinâmico (ex: Player) |
| `obj2` | `GameObject` | Objeto estático (ex: Parede/Chão) |

## CombatResolver2D

`class` from [`Core2D/Collision/CombatResolver2D.js`](../../Core2D/Collision/CombatResolver2D.js)

Stateless hitbox-versus-hurtbox resolver for data-driven 2D combat.

### Documented Methods

#### `static Resolve()`

Finds new attack contacts and emits optional game-owned hit handling.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `attackerBoxes` | `BoxController2D` | Attacking entity boxes. |
| `defenderBoxesList` | `BoxController2D\|BoxController2D[]` | Potential defenders. |
| `[options={}]` | `Object` | Default hit values and `onHit` callback. |

**Returns:** `Object[]` - Resolved hit records.

## ComboController

`class` from [`Core2D/Combat/ComboController.js`](../../Core2D/Combat/ComboController.js)

Small attack-chain state machine driven by a game's animation frames.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `owner` | `Object` | Combatant owning optional box data. |
| `[attacks=[]` | `Object[]` | ] - Ordered combo attack definitions. |
| `[options={}]` | `Object` | Input-buffer settings. |

## Component

`class` from [`CoreCross/Component.js`](../../CoreCross/Component.js)

Base attachable behavior unit for composing lightweight game entities.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[options]` | `Object` | Component settings. |
| `[options.enabled=true]` | `boolean` | Whether the owner should update this component. |

## Config

`class` from [`CoreCross/Config.js`](../../CoreCross/Config.js)

Loads and merges global and per-game JSON configuration files.

## DEBUG

`const` from [`CoreCross/Engine.js`](../../CoreCross/Engine.js)

Reads whether collision and hitbox debugging is enabled in config.

**Returns:** `boolean` - Current hitbox debug flag.

### Documented Methods

#### `addLevel()`

Adds a new level to the level handler.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `level` | `Level` | The level to add. |

**Returns:** `void`
#### `static OnStart()`

Initializes the engine, loads the first level, and starts the game loop.

**Returns:** `void`
#### `static GameLoop()`

Main game loop, updates the game state and renders the scene.

**Returns:** `void`
#### `static OnDrawn()`

Override this method to implement custom drawing logic.

**Returns:** `void`
#### `static RemoveLevel()`

Removes the specified level from the DOM.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `levelId` | `string` | The ID of the level to remove. |

**Returns:** `void`

## DebugMap

`class` from [`Core2D/Level/DebugMap.js`](../../Core2D/Level/DebugMap.js)

Small debug tile-map level helper rendered on a 2D screen.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `screen` | `Screen` | Destination canvas screen. |

## Deprecation

`class` from [`CoreCross/Deprecation.js`](../../CoreCross/Deprecation.js)

Reports use of compatibility APIs once per deprecated identifier. This keeps legacy projects running while guiding new games toward the maintained GameForgeJS API.

### Documented Methods

#### `static WarnOnce()`

Writes one browser warning for a deprecated API.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `apiName` | `string` | Deprecated public identifier. |
| `[replacement=""]` | `string` | Preferred replacement API. |

**Returns:** `void`

## dfs

`function` from [`CoreCross/Pathfinding/DFS.js`](../../CoreCross/Pathfinding/DFS.js)

🔵 2️⃣ DFS (Depth-First Search) Explora até o fundo ❌ Não garante menor caminho

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `grid` | `*` |  |
| `start` | `*` |  |
| `goal` | `*` |  |

## dijkstra

`function` from [`CoreCross/Pathfinding/Dijkstra.js`](../../CoreCross/Pathfinding/Dijkstra.js)

🔵 3️⃣ Dijkstra Explora com base em pesos ✅ Garante menor caminho Mais pesado que BFS

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `grid` | `*` |  |
| `start` | `*` |  |
| `goal` | `*` |  |

## Draw

`class` from [`Core2D/Graphics/Draw.js`](../../Core2D/Graphics/Draw.js)

Provides drawing utilities for 2D graphics. This class facilitates drawing operations on a given canvas, including text, rectangles, circles, and sprites.

**Returns:** `Object`

**Example**

```js
const draw = new Draw(screen);
draw.Color = 'red';
draw.DrawText('Hello World', 50, 50);
```

### Documented Methods

#### `SetTextAlign()`

Sets the text alignment for drawing text.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `align` | `string` | The text alignment (e.g., 'left', 'center', 'right'). |
#### `DrawText()`

Draws text on the canvas.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `text` | `string` | The text to be drawn. |
| `x` | `number` | The x-coordinate where the text will be drawn. |
| `y` | `number` | The y-coordinate where the text will be drawn. |
| `[maxWidth]` | `number` | The maximum width of the text. If not provided, there is no limit. |
#### `DrawRect()`

Draws a rectangle on the canvas.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `x` | `number` | The x-coordinate where the rectangle will be drawn. |
| `y` | `number` | The y-coordinate where the rectangle will be drawn. |
| `width` | `number` | The width of the rectangle. |
| `height` | `number` | The height of the rectangle. |
#### `DrawCircle()`

Draws a circle on the canvas.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `x` | `number` | The x-coordinate of the circle's center. |
| `y` | `number` | The y-coordinate of the circle's center. |
| `radius` | `number` | The radius of the circle. |
#### `DrawSprite()`

Draws an image or sprite on the canvas.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `sprite` | `HTMLImageElement \| string` | The image element or the URL of the sprite. |
| `x` | `number` | The x-coordinate where the sprite will be drawn. |
| `y` | `number` | The y-coordinate where the sprite will be drawn. |

## Engine

`class` from [`CoreCross/Engine.js`](../../CoreCross/Engine.js)

Main update and render loop orchestrator for the active GameForgeJS level. The engine publishes frame events consumed by systems such as `Input`.

### Documented Methods

#### `addLevel()`

Adds a new level to the level handler.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `level` | `Level` | The level to add. |

**Returns:** `void`
#### `static OnStart()`

Initializes the engine, loads the first level, and starts the game loop.

**Returns:** `void`
#### `static GameLoop()`

Main game loop, updates the game state and renders the scene.

**Returns:** `void`
#### `static OnDrawn()`

Override this method to implement custom drawing logic.

**Returns:** `void`
#### `static RemoveLevel()`

Removes the specified level from the DOM.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `levelId` | `string` | The ID of the level to remove. |

**Returns:** `void`

## EventEmitter

`class` from [`CoreCross/EventEmitter.js`](../../CoreCross/EventEmitter.js)

Minimal synchronous event publisher used across engine services.

### Documented Methods

#### `on()`

Registers a listener for an event name.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `event` | `string` | Event name. |
| `callback` | `Function` | Listener callback. |

**Returns:** `Function` - Unsubscribe callback.

## FloatingText

`class` from [`Core2D/Effects/FloatingText.js`](../../Core2D/Effects/FloatingText.js)

Canvas effect for short-lived labels such as damage or collectible feedback.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `screen` | `Screen` | Target 2D canvas screen. |

### Documented Methods

#### `Spawn()`

Activates this effect at a world or HUD position.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `text` | `string\|number` | Display content. |
| `x` | `number` | Horizontal position. |
| `y` | `number` | Vertical position. |
| `[options={}]` | `Object` | Velocity, lifetime and font overrides. |

**Returns:** `void`

## GameObject

`class` from [`Core2D/Objects/GameObject.js`](../../Core2D/Objects/GameObject.js)

Base movable 2D entity with sprite, collision and component support. Extend it for game-specific players, enemies and interactive objects.

## GamePad

`class` from [`CoreCross/Input/Gamepad.js`](../../CoreCross/Input/Gamepad.js)

Low-level browser gamepad state. Events are useful notifications, but the current state is always reconciled from navigator.getGamepads() each frame.

## GamepadAlias

`class` from [`CoreCross/Input/GamepadAlias.js`](../../CoreCross/Input/GamepadAlias.js)

Resolves readable controller button and axis names into browser Gamepad API indices. Profiles allow the same game's actions to match Xbox, PlayStation or Nintendo labels.

## GameViewport

`class` from [`CoreCross/Window/GameViewport.js`](../../CoreCross/Window/GameViewport.js)

Applies the responsive full-window canvas mode selected by a game config. Logical canvas coordinates remain unchanged; only presentation fills the available browser viewport.

### Documented Methods

#### `static IsFullScreen()`

Reads the active game's `screen.fullScreen` setting.

**Returns:** `boolean` - Whether full-window canvas presentation is enabled.

## getNeighbors

`function` from [`CoreCross/Pathfinding/Structure.js`](../../CoreCross/Pathfinding/Structure.js)

Returns walkable cardinal neighbors for a grid coordinate.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `grid` | `number[][]` | Grid whose zero entries can be walked. |
| `node` | `Object` | Current `{x, y}` coordinate. |

**Returns:** `Object[]` - Adjacent walkable coordinates.

## HealthComponent

`class` from [`CoreCross/Component.js`](../../CoreCross/Component.js)

Tracks hit points, invulnerability time and owner damage/death callbacks.

## Input

`class` from [`CoreCross/Input/Input.js`](../../CoreCross/Input/Input.js)

Manages per-frame keyboard, mouse, touch and polled gamepad input. Gamepad state is polled during the engine update so controllers already available to the browser are detected without reconnecting.

**Returns:** `Object`

**Example**

```js
import { Input } from "./Input.js";
if (Input.GetKey("ArrowUp")) {
// Execute some action
}
```

### Documented Methods

#### `static Update()`

Chamado pela Engine todo início de frame
#### `static LateUpdate()`

Chamado pela Engine todo fim de frame
#### `static GetKey()`

Check if a key is currently pressed

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `key` | `string` | The key code (e.g., "ArrowUp", "KeyA") |

**Returns:** `boolean` - True if the key is pressed, false otherwise
#### `static GetKeyDown()`

Check if a key was pressed in the current frame

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `key` | `string` | The key code (e.g., "ArrowUp", "KeyA") |

**Returns:** `boolean` - True if the key was pressed in the current frame, false otherwise
#### `static GetKeyUp()`

Check if a key was released in the current frame

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `key` | `string` | The key code (e.g., "ArrowUp", "KeyA") |

**Returns:** `boolean` - True if the key was released in the current frame, false otherwise
#### `static IsGamepadConnected()`

Tests whether a browser gamepad is currently available.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[padIndex=0]` | `number` | Browser gamepad slot. |

**Returns:** `boolean` - Whether that pad is connected.
#### `static GetConnectedGamepadIndices()`

Returns all currently connected browser gamepad slots.

**Returns:** `number[]` - Connected gamepad indices.
#### `static GetGamepadButton()`

Reads a held gamepad button using a configured alias or browser index token.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[padIndex=0]` | `number` | Browser gamepad slot. |
| `input` | `string\|number` | Alias such as `A` or `DPAD_LEFT`. |

**Returns:** `boolean` - Whether the button is held.
#### `static GetGamepadButtonDown()`

Reads a gamepad button press occurring in the current frame.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[padIndex=0]` | `number` | Browser gamepad slot. |
| `input` | `string\|number` | Configured alias or button index. |

**Returns:** `boolean` - Whether the button went down.
#### `static GetGamepadButtonUp()`

Reads a gamepad button release occurring in the current frame.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[padIndex=0]` | `number` | Browser gamepad slot. |
| `input` | `string\|number` | Configured alias or button index. |

**Returns:** `boolean` - Whether the button went up.
#### `static GetGamepadAxis()`

Reads an analog axis using an alias such as `LeftX`.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[padIndex=0]` | `number` | Browser gamepad slot. |
| `input` | `string\|number` | Configured axis alias or index. |

**Returns:** `number` - Axis value from -1 to 1, or zero if unavailable.
#### `static Keyboard()`

Builds an action mapping rule for keyboard input.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `input` | `string` | Keyboard event code. |

**Returns:** `Object` - Action mapping rule.
#### `static GamepadButton()`

Builds an action mapping rule for a gamepad button.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `input` | `string\|number` | Gamepad button alias or index. |
| `[padIndex=null]` | `number\|null` | Optional fixed gamepad slot. |

**Returns:** `Object` - Action mapping rule.
#### `static GamepadAxis()`

Builds an action mapping rule for one direction of an analog axis.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `input` | `string\|number` | Gamepad axis alias or index. |
| `[direction="positive"]` | `string\|number` | Positive/negative direction or sign. |
| `[padIndex=null]` | `number\|null` | Optional fixed gamepad slot. |

**Returns:** `Object` - Action mapping rule.

## JsonManifestComposer

`class` from [`CoreCross/Assets/JsonManifestComposer.js`](../../CoreCross/Assets/JsonManifestComposer.js)

Combines JSON game manifests referenced through their `compose` entries.

### Documented Methods

#### `static Compose()`

Recursively merges one loaded JSON asset with its child manifests.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `name` | `string` | AssetManager JSON key. |
| `[assets]` | `AssetManager` | Loaded asset store. |

**Returns:** `Object` - Composed manifest.

## key

`function` from [`CoreCross/Pathfinding/Structure.js`](../../CoreCross/Pathfinding/Structure.js)

Encodes a coordinate for pathfinding maps.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `node` | `Object` | Coordinate containing `x` and `y`. |

**Returns:** `string` - Stable coordinate key.

## KeyCode

`const` from [`CoreCross/Input/KeyCode.js`](../../CoreCross/Input/KeyCode.js)

Legacy-friendly names mapped to browser keyboard `code` values.

## Label

`class` from [`Core2D/UI/Label.js`](../../Core2D/UI/Label.js)

Simple canvas text label positioned by a rectangle.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `screen` | `Screen` | Screen used for drawing. |

### Documented Methods

#### `SetLabel()`

Sets the dimensions, position, and text content of the label.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `rect` | `Rectangle` |  |
| `text` | `string` |  |

**Returns:** `void`
#### `DrawLabel()`

Draws the label on the screen.

**Returns:** `void`

## Level

`class` from [`CoreCross/Level/Level.js`](../../CoreCross/Level/Level.js)

Base scene lifecycle container updated and drawn by the engine. Extend this class to own a game's entities and canvas screens.

### Documented Methods

#### `OnStart()`

Called when the level starts.

**Returns:** `void`
#### `OnUpdate()`

Called every frame to update the game state.

**Returns:** `void`
#### `OnFixedUpdate()`

Called at a fixed rate to update the game state.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `deltaTime` | `number` | The time elapsed since the last update. |

**Returns:** `void`
#### `OnDrawn()`

Called to draw the level.

**Returns:** `void`
#### `OnGUI()`

Called to draw the GUI.

**Returns:** `void`
#### `GetEntityByName()`

Get an entity by its name.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `name` | `string` |  |

**Returns:** `Object` - The entity with the specified name.
#### `AddEntity()`

Adds an entity to the level.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `entity` | `Object` | The entity to add. |

**Returns:** `void`
#### `RemoveEntity()`

Removes an entity from the level.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `entity` | `Object` | The entity to remove. |

**Returns:** `void`

## LevelHandler

`const` from [`CoreCross/Engine.js`](../../CoreCross/Engine.js)

Ordered level registry and current-level cursor used by `Engine`.

### Documented Methods

#### `addLevel()`

Adds a new level to the level handler.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `level` | `Level` | The level to add. |

**Returns:** `void`
#### `static OnStart()`

Initializes the engine, loads the first level, and starts the game loop.

**Returns:** `void`
#### `static GameLoop()`

Main game loop, updates the game state and renders the scene.

**Returns:** `void`
#### `static OnDrawn()`

Override this method to implement custom drawing logic.

**Returns:** `void`
#### `static RemoveLevel()`

Removes the specified level from the DOM.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `levelId` | `string` | The ID of the level to remove. |

**Returns:** `void`

## LifetimeComponent

`class` from [`CoreCross/Component.js`](../../CoreCross/Component.js)

Disables or removes short-lived owner objects after an elapsed duration.

## Logger

`class` from [`CoreCross/Logger.js`](../../CoreCross/Logger.js)

Console logging helpers for engine startup, diagnostics and HTTP requests.

## Mat4

`class` from [`CoreCross/Math/Mat4.js`](../../CoreCross/Math/Mat4.js)

Dependency-free four-by-four matrix math utilities for Render3D.

## MathExt

`class` from [`CoreCross/Math/MathExt.js`](../../CoreCross/Math/MathExt.js)

General scalar and 2D interpolation helpers shared by demos.

### Documented Methods

#### `static Lerp()`

Interpolação linear entre dois valores.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `a` | `number` | Valor inicial. |
| `b` | `number` | Valor final. |
| `t` | `number` | Fator de interpolação (0..1). |

**Returns:** `number`
#### `static LerpSnap()`

Lerp com snap: quando a diferença for menor que threshold, retorna b diretamente. Evita oscilação infinita no final da animação.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `a` | `number` |  |
| `b` | `number` |  |
| `t` | `number` |  |
| `threshold` | `number` |  |

**Returns:** `number`

## Menu

`class` from [`Core2D/Level/Menu.js`](../../Core2D/Level/Menu.js)

Basic keyboard-controlled menu level supplied as a simple 2D example.

## Mouse

`class` from [`CoreCross/Input/Mouse.js`](../../CoreCross/Input/Mouse.js)

Manages mouse input for the game framework.

**Returns:** `Object`

**Example**

```js
const mouse = new Mouse();
if (mouse.isClickDown(rect)) {
// Execute some action when mouse is clicked inside the rectangle
}
```

### Documented Methods

#### `mouseMoveHandler()`

Handles the mouse move event and updates the current position.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `event` | `MouseEvent` | The mouse move event |
#### `mouseDownHandler()`

Handles the mouse down event and updates the click position.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `event` | `MouseEvent` | The mouse down event |
#### `mouseUpHandler()`

Handles the mouse up event and updates the release position.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `event` | `MouseEvent` | The mouse up event |
#### `getPositionAbsolute()`

Gets the current absolute mouse position.

**Returns:** `Vector2D` - The current mouse position
#### `getPositionRelative()`

Gets the relative mouse position within a specified element.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `element` | `HTMLElement` | The HTML element to calculate the relative position against |

**Returns:** `Vector2D` - The relative mouse position
#### `getPositionClickDown()`

Gets the mouse position at the time of the last click down.

**Returns:** `Vector2D` - The position of the last mouse click down
#### `getPositionClickRelease()`

Gets the mouse position at the time of the last click release.

**Returns:** `Vector2D` - The position of the last mouse click release
#### `isClickDown()`

Checks if the mouse was clicked down within a specified rectangle.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `rect` | `Rectangle` | The rectangle to check against |

**Returns:** `boolean` - True if the mouse click was inside the rectangle, false otherwise
#### `isClickRelease()`

Checks if the mouse was released within a specified rectangle.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `rect` | `Rectangle` | The rectangle to check against |

**Returns:** `boolean` - True if the mouse release was inside the rectangle, false otherwise
#### `isColliding()`

Checks if a point is colliding with a rectangle.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `point` | `Vector2D` | The point to check |
| `rect` | `Rectangle` | The rectangle to check against |

**Returns:** `boolean` - True if the point is inside the rectangle, false otherwise

## NineSlice

`class` from [`Core2D/UI/NineSlice.js`](../../Core2D/UI/NineSlice.js)

Draws resizable UI panels from a nine-slice source image.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `screen` | `Screen` | Canvas screen. |
| `image` | `HTMLImageElement` | Panel texture. |
| `[cornerSize=16]` | `number` | Corner sample size in pixels. |

### Documented Methods

#### `DrawPanel()`

Draws the scalable panel on the screen.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `x` | `number` | Target X position |
| `y` | `number` | Target Y position |
| `finalWidth` | `number` | The total width you want the window to be |
| `finalHeight` | `number` | The total height you want the window to be |

## ObjectPool

`class` from [`CoreCross/ObjectPool.js`](../../CoreCross/ObjectPool.js)

Recicla GameObjects (como Tiros e Partículas) para evitar vazamento de memória.

### Documented Methods

#### `Get()`

Pega um objeto livre do Pool
#### `Release()`

Devolve o objeto para o Pool

## Particle

`class` from [`Core2D/Particle/Particle.js`](../../Core2D/Particle/Particle.js)

Short-lived moving dot used by the basic canvas particle system.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `x` | `number` | Initial horizontal coordinate. |
| `y` | `number` | Initial vertical coordinate. |

## ParticleSystem

`class` from [`Core2D/Particle/ParticleSystem.js`](../../Core2D/Particle/ParticleSystem.js)

Collection that emits, updates and draws simple `Particle` instances.

## Physic2D

`class` from [`Core2D/Collision/Physic2D.js`](../../Core2D/Collision/Physic2D.js)

Static helpers and coefficient values for basic 2D motion calculations.

## reconstructPath

`function` from [`CoreCross/Pathfinding/Structure.js`](../../CoreCross/Pathfinding/Structure.js)

Reconstructs a discovered route from predecessor entries.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `cameFrom` | `Object` | Coordinate-keyed predecessor map. |
| `current` | `Object` | Goal coordinate. |

**Returns:** `Object[]` - Ordered route.

## Rectangle

`class` from [`Core2D/Graphics/Rectangle.js`](../../Core2D/Graphics/Rectangle.js)

Axis-aligned 2D rectangle used for canvas layout and collision bounds.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `x` | `number` | Left coordinate. |
| `y` | `number` | Top coordinate. |
| `width` | `number` | Rectangle width. |
| `height` | `number` | Rectangle height. |

## ResourceManifestLoader

`class` from [`CoreCross/Assets/ResourceManifestLoader.js`](../../CoreCross/Assets/ResourceManifestLoader.js)

Queues images, audio, shaders, models and JSON declared by game resource manifests.

## Scene

`class` from [`Core2D/Scene/Scene.js`](../../Core2D/Scene/Scene.js)

This class is part of the EngineHtml5 framework and provides functionality to load and render 2D scenes based on maps created with the Tiled tool.

**Returns:** `Object`

**Example**

```js
const scene = new Scene("sceneId", screen);
scene.Load("mapName");
```

### Documented Methods

#### `LoadTileset()`

Load tileset and prepare for rendering layers.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `json` | `Object` | The JSON data from the Tiled tool. |

**Returns:** `void`
#### `RenderLayer()`

Render a single layer from the tileset.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `layer` | `Object` | The layer to render. |

**Returns:** `void`
#### `RenderLayers()`

Render all layers from the tileset.

**Returns:** `void`

## Screen

`class` from [`Core2D/Window/Screen.js`](../../Core2D/Window/Screen.js)

Creates and owns a two-dimensional gameplay canvas and draw context.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `id` | `string` | Canvas element identifier. |
| `[width]` | `number` | Logical canvas width. |
| `[height]` | `number` | Logical canvas height. |

### Documented Methods

#### `Init()`

Initialize scene

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `canvas` | `canvas` |  |
| `context` | `context` |  |
#### `GetSize()`

Return size screen
#### `GetwindowSize()`

Return size window browser
#### `Resize()`

Resize screen

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `width` | `width` |  |
| `height` | `height` |  |

## ScreenUI

`class` from [`Core2D/Window/ScreenUI.js`](../../Core2D/Window/ScreenUI.js)

Transparent overlay screen for GameForgeJS canvas-based UI above gameplay.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[id="gameCanvasUI"]` | `string` | UI canvas identifier. |
| `[width=800]` | `number` | Logical canvas width. |
| `[height=600]` | `number` | Logical canvas height. |
| `[zIndex=100]` | `number` | CSS stacking position. |

## Sprite

`class` from [`Core2D/Graphics/Sprite.js`](../../Core2D/Graphics/Sprite.js)

Canvas sprite renderer supporting sheet-based animation frames.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `screen` | `Screen` | Destination canvas screen. |
| `spriteSrc` | `string` | Loaded image key or sprite source. |

### Documented Methods

#### `Static()`

Draws a static sprite on the canvas.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[spriteSrc]` | `string` | The path to the sprite image. |
| `position` | `Vector2D` | The position on the canvas to draw the sprite. |
#### `Cliping()`

Draws a clipped portion of a sprite.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[spriteSrc]` | `string` | The path to the sprite image. |
| `position` | `Vector2D` | The position on the canvas to draw the sprite. |
| `size` | `Vector2D` | The size of the sprite to draw. |
| `cutPosition` | `Vector2D` | The position to start clipping from. |
| `cutSize` | `Vector2D` | The size of the clipping area. |
#### `Stretched()`

Draws the sprite stretched to the specified size on the canvas.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `[spriteSrc]` | `string` | The path to the sprite image. |
| `position` | `Vector2D` | The position on the canvas to draw the sprite. |
| `size` | `Vector2D` | The new size to stretch the sprite. |
#### `Animation()`

Draws an animated sprite based on the current index and row.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `spriteSrc` | `string` | The path to the sprite image. |
| `position` | `Vector2D` | The position on the canvas to draw the sprite. |
| `[direction='horizontal']` | `string` | The animation direction ('horizontal' or 'vertical'). |
| `[row=0]` | `number` | The row index for vertical animations. |
#### `Update()`

Updates the animation frame using the old updates-per-frame value as a 60 FPS timing reference.
#### `Reset()`

Resets the animation frame to the first frame.

## Store

`class` from [`CoreCross/Store.js`](../../CoreCross/Store.js)

This class is part of the EngineHtml5 framework and provides functionality to save and restore game state in the browser's local storage.

**Returns:** `Object`

**Example**

```js
var store = new Store();
store.SaveState({ level: 1, score: 1000 });
var state = store.RestoreState();
```

### Documented Methods

#### `SaveState()`

Saves the current state to local storage.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `state` | `Object` | The state object to be saved. |

**Returns:** `void`
#### `RestoreState()`

Restores the previously saved state from local storage.

**Returns:** `Object|null` - The saved state object or null if no state is found.
#### `ClearState()`

Clears the saved state from local storage.

**Returns:** `void`
#### `BackupState()`

Backs up the saved state to a different key in local storage.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `backupPrefix` | `string` | The prefix for the backup key. |

**Returns:** `void`

## TextBox

`class` from [`Core2D/UI/TextBox.js`](../../Core2D/UI/TextBox.js)

Canvas-drawn editable text field for in-game GameForgeJS UI.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `screen` | `Screen` | Screen used for drawing and pointer coordinates. |

### Documented Methods

#### `SetBox()`

Sets the dimensions and position of the text box.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `rect` | `Rectangle` |  |

**Returns:** `void`
#### `SetText()`

Sets the text content of the text box.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `text` | `string` |  |

**Returns:** `void`
#### `GetText()`

Gets the text content of the text box.

**Returns:** `string` - The current text in the text box.
#### `HandleInput()`

Handles keyboard input when the text box is active.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `event` | `KeyboardEvent` |  |

**Returns:** `void`
#### `Click()`

Activates the text box if it is clicked.

**Returns:** `void`
#### `DrawCursor()`

Draws the text box on the screen.

**Returns:** `void`

## TileCollision2D

`class` from [`Core2D/Collision/TileCollision2D.js`](../../Core2D/Collision/TileCollision2D.js)

Tile-map collision queries and axis-separated movement for rectangular actors.

### Documented Methods

#### `static Move()`

Moves a velocity-bearing rectangle and resolves nearby solid tiles.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `rect` | `Object` | Mutable rectangle with optional `vx` and `vy`. |
| `[options={}]` | `Object` | Grid query and movement settings. |

**Returns:** `Object` - Collision sides and grounded status.

## Touch

`class` from [`CoreCross/Input/Touch.js`](../../CoreCross/Input/Touch.js)

Manages touch input for the game framework.

**Returns:** `Object`

**Example**

```js
const touch = new Touch();
// Touch event listeners are automatically added
```

### Documented Methods

#### `touchStartHandler()`

Handles the touch start event and updates the start position.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `event` | `TouchEvent` | The touch start event |
#### `touchEndHandler()`

Handles the touch end event and updates the end position.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `event` | `TouchEvent` | The touch end event |
#### `touchMoveHandler()`

Handles the touch move event and updates the position.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `event` | `TouchEvent` | The touch move event |
#### `getTouchStartPosition()`

Gets the touch start position.

**Returns:** `Vector2D` - The start position of the last touch
#### `getTouchEndPosition()`

Gets the touch end position.

**Returns:** `Vector2D` - The end position of the last touch

## TransformComponent

`class` from [`CoreCross/Component.js`](../../CoreCross/Component.js)

Stores 2D position, rotation and scale and can mirror them onto its owner.

## UI

`class` from [`Core2D/UI/UI.js`](../../Core2D/UI/UI.js)

Convenience factory for canvas buttons, labels and text boxes on a screen.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `screen` | `Screen` | Screen receiving UI elements. |

### Documented Methods

#### `Button()`

Creates a button UI element and handles its click events.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `x` | `number` | The x-coordinate of the button's position. |
| `y` | `number` | The y-coordinate of the button's position. |
| `width` | `number` | The width of the button. |
| `height` | `number` | The height of the button. |
| `callBack` | `function` | The function to execute when the button is clicked. |

**Returns:** `Button` - The created button instance.
#### `TextBox()`

Creates a text box UI element where users can input text.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `x` | `number` | The x-coordinate of the text box's position. |
| `y` | `number` | The y-coordinate of the text box's position. |
| `width` | `number` | The width of the text box. |
| `height` | `number` | The height of the text box. |

**Returns:** `TextBox` - The created text box instance.
#### `Label()`

Creates a label UI element to display static text.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `x` | `number` | The x-coordinate of the label's position. |
| `y` | `number` | The y-coordinate of the label's position. |
| `text` | `string` | The text to display on the label. |

**Returns:** `Label` - The created label instance.

## UIWindow

`class` from [`Core2D/UI/UIWindow.js`](../../Core2D/UI/UIWindow.js)

Nine-slice canvas window that positions child labels relative to its frame. Subclass or compose this class for in-game panels rendered inside GameForgeJS rather than as external HTML.

### Documented Methods

#### `AddText()`

Adds text relative to the window's top-left corner

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `text` | `string` | Label content. |
| `offsetX` | `number` | Horizontal offset inside the window. |
| `offsetY` | `number` | Vertical offset inside the window. |
| `[color="#FFFFFF"]` | `string` | Canvas text color. |
| `[fontSize="20px"]` | `string` | CSS canvas font size. |
| `[fontStyle="Arial"]` | `string` | Canvas font family. |

**Returns:** `void`

## Util

`class` from [`CoreCross/Utils.js`](../../CoreCross/Utils.js)

That class was made, to compose the EngineHtml5 framework.

**Example**

```js
This is a static class, you can just use his functions
```

## Vector2D

`class` from [`CoreCross/Math/Vector2D.js`](../../CoreCross/Math/Vector2D.js)

That class was made, to compose the EngineHtml5 framework.

**Returns:** `Object`

**Example**

```js
var vector2D = new Vector2D(0, 0);
```

### Documented Methods

#### `GetValue()`

Get value x and y of Vector
#### `SetValue()`

Set value in Vector

## Vector3D

`class` from [`CoreCross/Math/Vector3D.js`](../../CoreCross/Math/Vector3D.js)

That class was made, to compose the EngineHtml5 framework.

**Returns:** `Object`

**Example**

```js
var vector3D = new Vector3D(0, 0, 0);
```

### Documented Methods

#### `GetValue()`

Get value x, y and z of Vector
#### `SetValue()`

Set value x, y and z of Vector

## VelocityComponent

`class` from [`CoreCross/Component.js`](../../CoreCross/Component.js)

Applies velocity, gravity and friction to an owner's 2D position.
